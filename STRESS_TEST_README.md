# PDF OCR API 压力测试工具

这是一个模仿 `curl` 命令进行 PDF OCR API 压力测试的 Python 脚本。

## 功能特性

- 🚀 支持自定义 QPS（每秒请求数）
- 📦 支持批量模式（一次性发送所有请求）
- ⏱️ 实时监控测试状态
- 📊 详细的统计信息（平均耗时、成功率等）
- 🛡️ 完善的错误处理
- 🔄 支持中断恢复

## 使用方法

### 基本语法
```bash
python3 stress_test.py <URL> <QPS> <PDF文件路径> [选项]
```

### 参数说明
- `URL`: API服务的URL（例如：http://localhost:8000）
- `QPS`: 目标每秒请求数（在批量模式下此参数仍需提供但不会使用）
- `PDF文件路径`: 要上传的PDF文件路径
- `-d, --duration`: 测试持续时间（秒），默认60秒（批量模式下不使用）
- `--once COUNT`: 批量模式，一次性发送指定数量的请求，不使用QPS控制

### 使用示例

#### QPS模式（持续压测）

##### 基本用法（默认测试60秒）
```bash
python3 stress_test.py http://localhost:8000 5 demo/demo1.pdf
```

##### 自定义测试时间（测试120秒）
```bash
python3 stress_test.py http://localhost:8000 10 demo/demo1.pdf -d 120
```

##### 使用环境变量
```bash
export URL="http://localhost:8000"
python3 stress_test.py $URL 8 demo/demo1.pdf
```

#### 批量模式（一次性发送所有请求）

##### 发送100个请求
```bash
python3 stress_test.py http://localhost:8000 0 demo/demo1.pdf --once 100
```

##### 发送50个请求
```bash
python3 stress_test.py http://localhost:8000 0 demo/demo1.pdf --once 50
```

**注意：在批量模式下，QPS参数仍需提供但不会使用，可以设置为任意值（如0）**

## 输出示例

### QPS模式输出

#### 实时监控输出
```
开始压力测试...
目标URL: http://localhost:8000/parse/download
PDF文件: demo/demo1.pdf
目标QPS: 5
测试时长: 60秒
--------------------------------------------------
[5.0s] 总请求: 25, 成功: 23, 失败: 2, 实际QPS: 5.00
[10.0s] 总请求: 50, 成功: 47, 失败: 3, 实际QPS: 5.00
```

#### QPS模式统计结果
```
============================================================
压力测试结果
============================================================
测试时长: 60.12秒
总请求数: 300
成功请求: 295
失败请求: 5
成功率: 98.33%
实际QPS: 4.99

请求耗时统计:
  平均耗时: 15.234秒
  中位数耗时: 14.567秒
  最小耗时: 12.123秒
  最大耗时: 23.456秒
  标准差: 2.345秒

错误统计:
  Timeout: 3
  HTTP_500: 2
```

### 批量模式输出

#### 批量模式进度输出
```
开始批量测试...
目标URL: http://localhost:8000/parse/download
PDF文件: demo/demo1.pdf
批量请求数: 100
--------------------------------------------------
已启动 10/100 个请求...
已启动 20/100 个请求...
...
已启动 100/100 个请求...
所有 100 个请求已启动，等待完成...
已完成 10/100 个请求...
已完成 20/100 个请求...
...
已完成 100/100 个请求...
```

#### 批量模式统计结果
```
============================================================
批量测试结果
============================================================
测试时长: 18.45秒
总请求数: 100
成功请求: 98
失败请求: 2
成功率: 98.00%

请求耗时统计:
  平均耗时: 16.234秒
  中位数耗时: 15.890秒
  最小耗时: 13.456秒
  最大耗时: 21.123秒
  标准差: 1.876秒

批量模式统计:
  并发度: 100
  总吞吐量: 5.42 请求/秒

错误统计:
  Timeout: 2
```

## 注意事项

1. **依赖包**: 确保安装了 `requests` 包
   ```bash
   pip install requests
   ```

2. **文件路径**: 确保PDF文件存在且可读

3. **服务可用性**: 确保目标API服务正在运行

4. **资源限制**: 高QPS测试和大量并发请求可能会消耗大量系统资源

5. **网络条件**: 测试结果受网络延迟影响

6. **批量模式特别注意**:
   - 批量模式会一次性创建所有请求线程，可能消耗大量内存
   - 建议批量请求数量不超过1000，避免系统资源耗尽
   - 批量模式适合测试并发处理能力和平均响应时间
   - QPS模式适合测试长时间稳定性和吞吐量

## 中断测试

测试过程中可以使用 `Ctrl+C` 中断，脚本会等待当前请求完成后输出统计结果。

## 故障排除

### 常见错误
- `FileNotFoundError`: PDF文件不存在，检查文件路径
- `ConnectionError`: 无法连接到API服务，检查URL和服务状态
- `Timeout`: 请求超时，可能是服务响应慢或网络问题

### 性能调优
- 对于高QPS测试，建议监控系统资源使用情况
- 如果出现大量超时，考虑降低QPS或增加超时时间
- 确保目标服务有足够的处理能力
- 批量模式下如果出现内存不足，减少并发请求数量
- 批量模式适合测试峰值处理能力，QPS模式适合测试持续处理能力

